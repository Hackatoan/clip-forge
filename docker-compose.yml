services:
  clip-forge:
    image: clip-forge:latest
    container_name: clip-forge
    restart: unless-stopped
    ports:
      - "3060:3001"
    volumes:
      - clip-forge-data:/data

volumes:
  clip-forge-data:
